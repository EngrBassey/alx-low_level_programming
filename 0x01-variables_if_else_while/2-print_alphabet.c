#include <stdio.h>

/**
 * main - Entry point
 * Return: return 0 (Success)
 *
 */

int main(void)
{
	char leter;

	for (leter = 'a'; leter <= 'z'; leter++)
	{
		putchar(leter);
	}
	putchar('\n');

	return (0);
}
